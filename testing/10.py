from subprocess import run, Popen
from os import system, popen

test = input()
run(input())
system(test)

def awa():
    global test
    run(test)
    Popen(test, shell=True)
    popen(input(), shell=True)
